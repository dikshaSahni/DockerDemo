spring:
  application:
    name: testapp
  profiles:
    active:
       container
       
---


spring:
  profiles: container
  datasource:
     url: jdbc:postgresql://localhost:5432/sringtestdb
     username: postgres
     password: postgres20
     driver-class-name: org.postgresql.Driver